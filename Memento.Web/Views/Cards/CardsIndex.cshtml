@using Memento.Models.ViewModels;

@model IEnumerable<ViewCardViewModel>

@{
    ViewBag.Title = "Cards Index";
}

<h2>Index</h2>
<hr />

@foreach (var item in Model)
{
    Html.RenderPartial("_Question", new AnswerCardViewModel { Question = item.Text });
}
